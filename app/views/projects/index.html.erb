<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"></script>
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>CS3300 Portfolio Project</title>
  <style> 
      .notice {
          color: orange;
      }
      .project {
          justify-content: center;
      }
      .card{
          background-color: gray;
          margin-right:20px!important
          color: orange;
      }
      

  </style> 
  </head>

  <body class="bg-dark">
    <div class="container p-4 bg-info text-white" style="width: auto;">
    <!--- Explain what the block of code below this line is doing --->
    <% if user_signed_in? %>
      <div> Welcome <%= current_user.email %> </div>
      <div class="col  d-flex justify-content-end align-items-start">
          <%= link_to "New project", new_project_path , class: "btn btn-primary text-dark  bg-light"%>
          <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "btn btn-primary text-dark  bg-light"%> 
      <% else %>
        <div>ㅤㅤㅤ </div>
        <div class="col  d-flex justify-content-end align-items-start">
         <%= link_to "Sign In", "/users/sign_in" , class: "btn btn-primary text-dark  bg-light"%>
      <% end %>

    </div>
    <div class="page-header">
      <h1>CS 3300 Portfolio Project - Fall 2022</h1>
    </div>
    <p> Hello! I'm a <a href="https://eas.uccs.edu/cs/academics/undergraduate-programs/bachelor-of-science-in-computer-science">CS</a> student at UCCS in Colorado</p>
    </div>
    <div>
    <div>
    </div>          
         <div class = "container p-4">  
             <div class="card-group">
            <% @projects.each do |project| %>
            <div class="card" style="width: auto;">
            <%= render project %>
            <%= link_to "Show this project", project , class: "mt-auto btn btn-info" %>
            </div>
            <% end %>
          </div>
          </div>
          </div>
  </body>
</html>
            
